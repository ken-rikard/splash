---
phase: 03-favorites-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/hooks/useFavorites.ts
  - ui/src/components/shared/FavoriteButton.tsx
  - ui/src/features/dashboard/FilterBar.tsx
  - ui/src/features/dashboard/RiverCard.tsx
  - ui/src/features/dashboard/DashboardPage.tsx
autonomous: true
requirements:
  - FAV-01
  - FAV-02
  - PWA-01

must_haves:
  truths:
    - "User can tap a heart icon on any RiverCard to toggle it as a favorite"
    - "Favorited rivers show a cyan-filled heart; unfavorited show an outline heart"
    - "Favorites persist across page reload (survive refresh and tab close)"
    - "Dashboard filters to 'Favorites only' when user selects that tab"
    - "FilterBar shows favorites count badge and only appears when user has ≥1 favorite"
    - "When filter is 'Favorites' and none saved, user sees 'No favorites yet' empty state"
  artifacts:
    - path: "ui/src/hooks/useFavorites.ts"
      provides: "localStorage-backed favorites persistence hook"
      exports: ["useFavorites"]
    - path: "ui/src/components/shared/FavoriteButton.tsx"
      provides: "Reusable heart toggle button with aria-label/aria-pressed"
      exports: ["FavoriteButton"]
    - path: "ui/src/features/dashboard/FilterBar.tsx"
      provides: "Segmented pill control for All/Favorites filter"
      exports: ["FilterBar"]
    - path: "ui/src/features/dashboard/RiverCard.tsx"
      provides: "River card with isFavorite state and FavoriteButton"
      exports: ["RiverCard"]
    - path: "ui/src/features/dashboard/DashboardPage.tsx"
      provides: "Dashboard with filter state, FilterBar, favorites integration"
  key_links:
    - from: "useFavorites"
      to: "localStorage"
      via: "getItem/setItem under key 'splash-favorites'"
    - from: "RiverCard"
      to: "FavoriteButton"
      via: "import + render in header row"
    - from: "DashboardPage"
      to: "useFavorites"
      via: "hooks call, returns { isFavorite, toggleFavorite, count }"
    - from: "DashboardPage"
      to: "FilterBar"
      via: "conditionally rendered when count > 0"
    - from: "DashboardPage"
      to: "RiverCard"
      via: "passes isFavorite(id) and toggleFavorite as props"
---

<objective>
**As a** river paddler, **I want to** save rivers to a favorites list from the dashboard and toggle a filter to see only my favorites, **so that** I can quickly check conditions on the rivers I care about.

**Purpose:** Deliver the primary favorites interaction surface — the dashboard is where users spend most of their time and where they need quick access to favoriting.

**Output:**
- `ui/src/hooks/useFavorites.ts` — localStorage-backed favorites hook with cross-tab sync
- `ui/src/components/shared/FavoriteButton.tsx` — reusable heart toggle button
- `ui/src/features/dashboard/FilterBar.tsx` — segmented pill filter control
- Modified `ui/src/features/dashboard/RiverCard.tsx` — accepts `isFavorite`/`onToggleFavorite` props, renders FavoriteButton
- Modified `ui/src/features/dashboard/DashboardPage.tsx` — filter state, favorites integration, "No favorites yet" empty state
</objective>

<execution_context>
@/home/kenr/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/kenr/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-favorites-engine/03-RESEARCH.md
@.planning/phases/03-favorites-engine/03-UI-SPEC.md

@ui/src/hooks/useRivers.ts
@ui/src/hooks/useRiver.ts
@ui/src/features/dashboard/RiverCard.tsx
@ui/src/features/dashboard/DashboardPage.tsx
@ui/src/features/dashboard/EmptyState.tsx
@ui/src/components/ui/button.tsx
@ui/src/lib/utils.ts
@ui/src/types/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create useFavorites hook and FavoriteButton component</name>
  <files>ui/src/hooks/useFavorites.ts, ui/src/components/shared/FavoriteButton.tsx</files>

  <read_first>
    @ui/src/hooks/useRivers.ts — follow same hook pattern (no external deps, function-call-scoped state, return object)
    @ui/src/hooks/useRiver.ts — verify hook return pattern consistency
    @ui/src/components/ui/button.tsx — understand Button API (variant="ghost", size="icon-sm")
    @ui/src/types/index.ts — RiverData type for id field
    @ui/src/lib/utils.ts — cn utility for class merging
    @ui/src/index.css — touch-target class definition (min-h-[44px] min-w-[44px])
  </read_first>

  <action>
    Create `ui/src/hooks/useFavorites.ts`:

    - STORAGE_KEY constant set to `'splash-favorites'` (per D-01)
    - useState with lazy initializer: try/catch around localStorage.getItem(STORAGE_KEY), JSON.parse, new Set; fallback to empty Set on any error (SSR safety, malformed data per threat model)
    - useEffect to write back: `localStorage.setItem(STORAGE_KEY, JSON.stringify([...favorites]))` wrapped in try/catch for quota exceeded (per threat model, degrade gracefully)
    - Cross-tab sync useEffect: window.addEventListener('storage', ...), parse newValue, setFavorites(new Set(...)) — only when e.key === STORAGE_KEY, with try/catch
    - isFavorite(id): useCallback returning favorites.has(id)
    - toggleFavorite(id): useCallback that setFavorites(prev => { const next = new Set(prev); next.has(id) ? next.delete(id) : next.add(id); return next })
    - Return { favorites: Set<string>, isFavorite, toggleFavorite, count: number } matching interface from RESEARCH.md
    - Hook naming and structure must match existing useRivers/useRiver patterns (0 external state, no context providers)

    Create `ui/src/components/shared/FavoriteButton.tsx`:

    - Props: riverId: string, isFavorite: boolean, onToggle: (id: string) => void, size?: 'sm' | 'md' (default 'md'), className?: string
    - Size mapping: 'sm' → `size-6`, 'md' → `size-7`
    - Import Heart from lucide-react, Button from @/components/ui/button, cn from @/lib/utils
    - Button variant="ghost", size="icon-sm", className merges touch-target (per D-06: min-h-11 44px via `.touch-target` class defined in index.css)
    - aria-label: `isFavorite ? 'Remove from favorites' : 'Add to favorites'` (dynamic per state, matches UI spec copywriting contract)
    - aria-pressed={isFavorite}
    - onClick handler: e.preventDefault() + e.stopPropagation() (critical: RiverCard is inside a Link, must not navigate)
    - Heart icon: h-4 w-4, transition-all duration-150 ease-out
      - isFavorite: text-accent-water fill-current scale-110
      - !isFavorite: text-slate-400
    - Hover states via Tailwind: unfav hover `hover:text-slate-300`, fav hover `hover:text-cyan-300`
    - Use cn() for className merging, not template literals
    - Export as named export `FavoriteButton`
  </action>

  <acceptance_criteria>
    - Source assertion: ui/src/hooks/useFavorites.ts exists and exports `function useFavorites()`
    - Source assertion: ui/src/components/shared/FavoriteButton.tsx exists and exports `function FavoriteButton()`
    - Source assertion: useFavorites returns an object with properties `favorites`, `isFavorite`, `toggleFavorite`, `count`
    - Source assertion: FavoriteButton accepts props `riverId`, `isFavorite`, `onToggle`, optional `size` and `className`
    - Source assertion: FavoriteButton renders a Button with aria-pressed boolean attribute
    - Source assertion: FavoriteButton renders Heart icon from lucide-react
    - Source assertion: useFavorites references literal string `'splash-favorites'` as the localStorage key
    - Source assertion: useFavorites has try/catch wrapping both getItem and setItem calls
    - Source assertion: useFavorites has window.addEventListener('storage', ...) for cross-tab sync
    - Source assertion: FavoriteButton has e.preventDefault() and e.stopPropagation() in its click handler
    - Source assertion: FavoriteButton has `touch-target` in its className
    - CLI: `cd ui && npx tsc --noEmit` passes (no type errors)
  </acceptance_criteria>

  <verify>
    <automated>cd ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <done>useFavorites hook and FavoriteButton component created, TypeScript compiles without errors, cross-tab sync listeners registered, try/catch guards on all localStorage access</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Modify RiverCard with favorite props and FavoriteButton</name>
  <files>ui/src/features/dashboard/RiverCard.tsx</files>

  <read_first>
    @ui/src/features/dashboard/RiverCard.tsx — current implementation with Link wrapper, StatusDot, DangerLevelBar, ChevronRight
    @ui/src/types/index.ts — RiverData type for `river.id`
    @ui/src/components/shared/FavoriteButton.tsx — just created in Task 1
  </read_first>

  <action>
    Modify `ui/src/features/dashboard/RiverCard.tsx`:

    - Import FavoriteButton from '@/components/shared/FavoriteButton'
    - Add two new props to the component function parameters: `isFavorite: boolean` and `onToggleFavorite: (id: string) => void`
    - In the header row (the flex items-start justify-between div at the top of card content):
      - The current structure: [StatusDot+name group] + [currentLevel group]
      - NEW structure: [StatusDot+name group] + [FavoriteButton + currentLevel group]
      - Wrap the currentLevel <p> and the new FavoriteButton in a new div with `flex items-center gap-2 flex-shrink-0 ml-4`
    - Place FavoriteButton BEFORE the currentLevel <p> inside that new wrapper div
    - Pass to FavoriteButton: riverId={river.id}, isFavorite={isFavorite}, onToggle={onToggleFavorite}
    - Do NOT modify the Link wrapper, DangerLevelBar, StatusDot, animation delay, or any other existing structure
    - The `e.stopPropagation()` inside FavoriteButton's click handler prevents the Link from navigating when heart is tapped
  </action>

  <acceptance_criteria>
    - Source assertion: RiverCard function signature includes `isFavorite: boolean` and `onToggleFavorite: (id: string) => void` parameters
    - Source assertion: RiverCard imports FavoriteButton from '@/components/shared/FavoriteButton'
    - Source assertion: The header row contains `<FavoriteButton riverId={river.id} isFavorite={isFavorite} onToggle={onToggleFavorite} />`
    - Source assertion: FavoriteButton and currentLevel are wrapped in a shared container div
    - CLI: `cd ui && npx tsc --noEmit` passes
    - CLI: `grep -c "import.*FavoriteButton" ui/src/features/dashboard/RiverCard.tsx` returns 1
    - Behavior: clicking the heart icon on a RiverCard does not navigate to the detail page (verified by e.stopPropagation in button)
  </acceptance_criteria>

  <verify>
    <automated>cd ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <done>RiverCard accepts favorite props, renders FavoriteButton in header row, TypeScript compiles, heart toggle does not trigger Link navigation</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create FilterBar component and modify DashboardPage with favorites integration</name>
  <files>ui/src/features/dashboard/FilterBar.tsx, ui/src/features/dashboard/DashboardPage.tsx</files>

  <read_first>
    @ui/src/features/dashboard/FilterBar.tsx — does not exist yet, will create
    @ui/src/features/dashboard/DashboardPage.tsx — current structure: loading skeleton, error, empty, connected states
    @ui/src/features/dashboard/EmptyState.tsx — existing "No rivers yet" state for reference
    @ui/src/lib/utils.ts — cn utility
    @ui/src/hooks/useFavorites.ts — just created in Task 1
    @ui/src/features/dashboard/RiverCard.tsx — just modified in Task 2 (new isFavorite/onToggleFavorite props)
  </read_first>

  <action>
    Create `ui/src/features/dashboard/FilterBar.tsx`:

    - Define type `FilterValue = 'all' | 'favorites'`
    - Props interface FilterBarProps: filter: FilterValue, onChange: (value: FilterValue) => void, favoritesCount: number
    - Export FilterBar as named export
    - Import cn from @/lib/utils
    - Root container: div with `role="tablist"` and `aria-label="Filter rivers by favorites"`, className `inline-flex items-center rounded-none border border-white/5 bg-surface p-0.5 gap-0`
    - "All Rivers" button: role="tab", aria-selected={filter === 'all'}, onClick → onChange('all'), className with cn:
      - `inline-flex items-center justify-center px-3 py-1.5 text-[11px] font-semibold uppercase tracking-widest rounded-none transition-colors duration-150 min-h-[44px]`
      - Active: `bg-surface-elevated text-white`, Inactive: `text-slate-400 hover:text-slate-300`
    - Separator: div with `w-px h-4 bg-white/5` and aria-hidden="true"
    - "Favorites" button: same structure as All Rivers + aria-selected={filter === 'favorites'}
      - After the "Favorites" label text, add a `<span className="ml-1.5 text-[10px] text-slate-500">{favoritesCount}</span>` for the count badge

    Modify `ui/src/features/dashboard/DashboardPage.tsx`:

    - Add imports: `import { useFavorites } from '@/hooks/useFavorites'`, `import { FilterBar } from './FilterBar'`, `import { Heart } from 'lucide-react'`
    - Add state inside DashboardPage: `const [filter, setFilter] = useState<'all' | 'favorites'>('all')`
    - Add: `const { isFavorite, toggleFavorite, count } = useFavorites()`
    - Add: `const displayRivers = filter === 'favorites' ? rivers.filter(r => isFavorite(r.id)) : rivers`
    - In each return block (loading, error, empty, connected), the `displayRivers` is only used in the connected block
    - In the connected block (where rivers.length > 0):
      - Change the heading row from `<h1>...</h1>` to a flex container: `<div className="flex items-start justify-between mb-8 flex-wrap gap-3"><h1>...</h1>{count > 0 && <FilterBar filter={filter} onChange={setFilter} favoritesCount={count} />}</div>`
      - Before the river grid, add the "No favorites yet" empty state: if filter === 'favorites' && displayRivers.length === 0, render inline:
        ```tsx
        <div className="flex flex-col items-center justify-center py-20 text-center">
          <Heart className="h-12 w-12 text-slate-600 mb-4" />
          <h2 className="text-lg font-display font-semibold text-white mb-1">No favorites yet</h2>
          <p className="text-sm text-slate-400 max-w-sm">Tap the ♡ on any river to add it to your favorites.</p>
        </div>
        ```
      - Replace `rivers.map(...)` with `displayRivers.map(...)`
      - Pass `isFavorite={isFavorite(river.id)}` and `onToggleFavorite={toggleFavorite}` to each RiverCard
    - Keep all existing states intact (loading, error, empty with EmptyState)
    - Do NOT modify the skeleton array, error state, or EmptyState rendering
  </action>

  <acceptance_criteria>
    - Source assertion: ui/src/features/dashboard/FilterBar.tsx exports function FilterBar() with FilterValue type
    - Source assertion: FilterBar has role="tablist", buttons have role="tab" and aria-selected attributes
    - Source assertion: DashboardPage imports useFavorites and FilterBar
    - Source assertion: DashboardPage has `const [filter, setFilter] = useState<'all' | 'favorites'>('all')`
    - Source assertion: DashboardPage has `const { isFavorite, toggleFavorite, count } = useFavorites()`
    - Source assertion: DashboardPage has `const displayRivers = filter === 'favorites' ? rivers.filter(r => isFavorite(r.id)) : rivers`
    - Source assertion: DashboardPage renders FilterBar only when `count > 0`
    - Source assertion: Each RiverCard receives `isFavorite={isFavorite(river.id)}` and `onToggleFavorite={toggleFavorite}`
    - Source assertion: Rhe connected block uses displayRivers.map instead of rivers.map
    - Source assertion: "No favorites yet" empty state renders when filter === 'favorites' && displayRivers.length === 0
    - CLI: `cd ui && npx tsc --noEmit` passes
  </acceptance_criteria>

  <verify>
    <automated>cd ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <done>DashboardPage integrates favorites filter + FavoriteButton in RiverCards + "No favorites yet" empty state, FilterBar renders conditionally, TypeScript compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage read/write | Untrusted: stored data may be malformed or quota may be exceeded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-FAV-01 | Data Tampering | localStorage read (getItem) | mitigate | try/catch in useFavorites lazy initializer — parse failure returns empty Set |
| T-FAV-02 | Denial of Service | localStorage write (setItem) | mitigate | try/catch in useEffect write-back — quota exceeded degrades to in-memory-only with console.warn |
| T-FAV-03 | Information Disclosure | Favorites stored unencrypted | accept | Data is river IDs only — no PII or secrets. localStorage encryption unnecessary per RESEARCH.md security domain |
</threat_model>

<verification>
- [ ] `cd ui && npx tsc --noEmit` passes with zero errors
- [ ] All import paths resolve correctly (@/hooks/useFavorites, @/components/shared/FavoriteButton, etc.)
- [ ] No new npm dependencies added
- [ ] No new routes added
- [ ] No server-side code modified
</verification>

<success_criteria>
1. User can tap heart icon on any RiverCard to toggle favorite state
2. Favorited heart is cyan-filled (text-accent-water fill-current), unfavorited is outline (text-slate-400)
3. Reloading the page preserves favorite state
4. Dashboard heading row shows FilterBar ("All Rivers" / "Favorites") when user has ≥1 favorite
5. FilterBar shows total favorites count next to "Favorites" label
6. Selecting "Favorites" filter shows only favorited rivers
7. When filter is "Favorites" and none saved, user sees Heart icon + "No favorites yet" + guidance text
8. Heart toggle does not navigate to river detail page (e.stopPropagation works)
9. All localStorage operations wrapped in try/catch
10. Cross-tab sync via storage event listener
</success_criteria>

<output>
Create `.planning/phases/03-favorites-engine/03-01-SUMMARY.md` when done
</output>
