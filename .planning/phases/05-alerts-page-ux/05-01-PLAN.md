---
phase: 05-alerts-page-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/events.ts
  - src/core/alert-engine.ts
  - src/index.ts
  - server.ts
  - tests/core/alert-engine.test.ts
autonomous: true
requirements:
  - ALERT-03
  - ALERT-04

must_haves:
  truths:
    - "AlertEngine.evaluate() returns a diff { triggered, resolved } describing state transitions"
    - "ScraperEventBus emits alert-trigger and alert-resolve events during data-update cycles"
    - "SSE bridge at /api/events forwards alert-trigger and alert-resolve events to connected clients"
    - "GET /api/alerts/config/:id returns a single AlertConfig or 404"
    - "All existing alert-engine tests still pass with the new evaluate() return type"
  artifacts:
    - path: "src/core/events.ts"
      provides: "ScraperEventMap with alert-trigger and alert-resolve event types"
      contains: "'alert-trigger' | 'alert-resolve'"
    - path: "src/core/alert-engine.ts"
      provides: "evaluate() returning { triggered: ActiveAlert[]; resolved: string[] }"
      contains: "return { triggered, resolved }"
    - path: "src/index.ts"
      provides: "Event emission from data-update handler for triggered/resolved alerts"
      contains: "emit('alert-trigger'"
    - path: "server.ts"
      provides: "GET /api/alerts/config/:id endpoint and SSE forwarding for alert events"
      contains: "app.get('/api/alerts/config/:id'"
  key_links:
    - from: "src/index.ts"
      to: "src/core/alert-engine.ts"
      via: "evaluate() call captures diff"
      pattern: "alertEngine\\.evaluate"
    - from: "src/index.ts"
      to: "ScraperEventBus"
      via: "emit('alert-trigger') and emit('alert-resolve')"
      pattern: "eventBus\\.emit\\('alert-(trigger|resolve)'"
    - from: "server.ts"
      to: "ScraperEventBus"
      via: ".on('alert-trigger') and .on('alert-resolve') SSE listeners"
      pattern: "eventBus\\.on\\('alert-(trigger|resolve)'"
---

<objective>
Server-side alert events infrastructure: add `alert-trigger`/`alert-resolve` event types to ScraperEventMap, modify `AlertEngine.evaluate()` to return a diff of triggered/resolved alerts, emit events from the data-update handler in `src/index.ts`, forward alert events through the SSE bridge in `server.ts`, and add `GET /api/alerts/config/:id` REST endpoint.

Purpose: Enable real-time alert notification delivery to connected clients. This is the server-side foundation that Plan 05-02 (client-side) consumes.

Output: ScraperEventBus with two new event types, evaluate() returning a diff, event emission in the scrape cycle, SSE forwarding, and a new REST endpoint.
</objective>

<execution_context>
@/home/kenr/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/kenr/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-alerts-page-ux/05-RESEARCH.md

# Pattern reference — current source files
@src/core/events.ts
@src/core/alert-engine.ts
@src/index.ts
@server.ts
@src/core/types.ts
@tests/core/alert-engine.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add alert event types to ScraperEventMap + modify evaluate() to return triggered/resolved diff</name>
  <files>
    src/core/events.ts
    src/core/alert-engine.ts
    tests/core/alert-engine.test.ts
  </files>
  <read_first>
    @src/core/events.ts
    @src/core/alert-engine.ts
    @src/core/types.ts
    @tests/core/alert-engine.test.ts
  </read_first>
  <action>
    Step 1 — Add event types to `src/core/events.ts`:
    - Add `import type { ActiveAlert } from './types.js'` at the top (after existing imports)
    - Add `'alert-trigger': (alert: ActiveAlert) => void` and `'alert-resolve': (info: { riverId: string }) => void` as new entries inside `ScraperEventMap` (after line 8 `'status-change'` line)

    Step 2 — Modify `src/core/alert-engine.ts` `evaluate()` method:
    - Change return type from `void` to `{ triggered: ActiveAlert[]; resolved: string[] }`
    - Before the `for` loop, initialize `const triggered: ActiveAlert[] = []` and `const resolved: string[] = []`
    - In the `!config || !config.enabled` branch: instead of unconditionally deleting, check `this.activeAlerts.has(river.id)` first, and if it does, delete and `resolved.push(river.id)` — this is a behavioral change from Phase 4 where disabled configs always deleted silently
    - In the `isTriggered && !hasActive` branch: after `this.activeAlerts.set(...)`, add `triggered.push(alert)` where `alert` is the object being set
    - In the `!isTriggered && hasActive` branch: after `this.activeAlerts.delete(river.id)`, add `resolved.push(river.id)`
    - After the `for` loop, add `return { triggered, resolved }`

    Step 3 — Add new test cases to `tests/core/alert-engine.test.ts`:
    - Add import: `ActiveAlert` is already imported via `AlertConfig, RiverData, AlertLevel`
    - Under `describe('evaluation — level-based')`:
      - Test: `evaluate() returns triggered array with new alert` — set config level=3, evaluate with alertLevel=5 river, assert `result.triggered` has length 1, `result.triggered[0].riverId` equals the river ID, `result.resolved` is empty
    - Under `describe('evaluation — resolution')`:
      - Test: `evaluate() returns resolved array when alert drops below threshold` — trigger first, then evaluate with lower level, assert `result.resolved` has length 1 with the riverId, `result.triggered` is empty
      - Test: `evaluate() returns empty arrays when no state change` — evaluate with no config change, assert both arrays are empty
    - Under `describe('edge cases')`:
      - Test: `evaluate() returns resolved for disabled config that had active alert` — trigger with level=1 enabled, then setConfig with enabled=false, evaluate again, assert resolved contains the riverId
    - Do NOT modify existing test cases — they should still pass because JS ignores unused return values
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose tests/core/alert-engine.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `evaluate()` returns `{ triggered: ActiveAlert[], resolved: string[] }` instead of `void`
    - Behavior: New trigger of a river returns that river's `ActiveAlert` in `triggered` array
    - Behavior: Resolution of a triggered river returns that river's `riverId` in `resolved` array
    - Behavior: No state transition returns empty `triggered` and `resolved` arrays
    - Behavior: Disabled config with active alert triggers resolution (returns `riverId` in `resolved`)
    - Regression: All 17 existing test cases still pass
    - Source: `src/core/events.ts` contains `'alert-trigger': (alert: ActiveAlert) => void` and `'alert-resolve': (info: { riverId: string }) => void`
    - Source: `src/core/alert-engine.ts` contains `return { triggered, resolved }`
    - Source: `src/core/alert-engine.ts`'s `!config || !config.enabled` branch conditionally resolves only when `this.activeAlerts.has(river.id)`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire event emission in data-update handler, forward through SSE bridge, add GET /api/alerts/config/:id endpoint</name>
  <files>
    src/index.ts
    server.ts
  </files>
  <read_first>
    @src/index.ts
    @server.ts
    @src/core/types.ts
  </read_first>
  <action>
    Step 1 — Modify `src/index.ts` data-update handler:
    - Change line 18 from `alertEngine.evaluate(rivers)` to capture the return value: `const { triggered, resolved } = alertEngine.evaluate(rivers)`
    - After the evaluate() call and before the `console.log`, add:
      - `for (const alert of triggered) { engine.eventBus.emit('alert-trigger', alert) }`
      - `for (const riverId of resolved) { engine.eventBus.emit('alert-resolve', { riverId }) }`
    - Keep the existing `console.log` line unchanged

    Step 2 — Modify `server.ts`:
    - Update the type import on line 5 to include `ActiveAlert`: change `import type { RiverData, AlertLevel }` to `import type { RiverData, AlertLevel, ActiveAlert }`
    - After line 101 (the `GET /api/alerts/active` endpoint), add the new endpoint `GET /api/alerts/config/:id`:
      - Route: `app.get('/api/alerts/config/:id', (req, res) => { ... })`
      - Call `alertEngine.getConfig(req.params.id)` 
      - If falsy, return `res.status(404).json({ error: 'No alert config for this river' })` with explicit return
      - Otherwise call `res.json(config)`
      - Follow the same pattern as `GET /api/rivers/:id` (lines 41-48)

    Step 3 — Add SSE listeners inside the `/api/events` handler in `server.ts`:
    - After the `onStale` listener declaration (line 124), add two new listener declarations:
      - `const onAlertTrigger = (alert: ActiveAlert) => { res.write(\`event: alert-trigger\ndata: ${JSON.stringify(alert)}\n\n\`) }`
      - `const onAlertResolve = (info: { riverId: string }) => { res.write(\`event: alert-resolve\ndata: ${JSON.stringify(info)}\n\n\`) }`
    - Register the listeners with `engine.eventBus.on('alert-trigger', onAlertTrigger)` and `engine.eventBus.on('alert-resolve', onAlertResolve)` — place them after the existing `.on()` calls (after line 128)
    - In the `req.on('close')` cleanup (after line 137-139 `removeListener` calls), add:
      - `engine.eventBus.removeListener('alert-trigger', onAlertTrigger as any)`
      - `engine.eventBus.removeListener('alert-resolve', onAlertResolve as any)`
    - Follow the exact same listener/cleanup pattern as the existing `data-update`, `error`, and `stale` listeners (lines 114-128 and 135-139)

    Step 4 — Verify:
    - Run `npx tsc --noEmit` — must compile with zero errors
    - Run `npx vitest run --reporter=verbose` — all tests must pass (existing + new)
    - Verify GET /api/alerts/config/:id returns 200 with config or 404
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/index.ts` contains `eventBus.emit('alert-trigger', alert)` inside the data-update handler
    - Source: `src/index.ts` contains `eventBus.emit('alert-resolve', { riverId })` inside the data-update handler
    - Source: `server.ts` contains `app.get('/api/alerts/config/:id'` endpoint definition
    - Source: `server.ts` contains `engine.eventBus.on('alert-trigger', onAlertTrigger)` and `engine.eventBus.on('alert-resolve', onAlertResolve)` inside the SSE handler
    - Source: `server.ts` contains `engine.eventBus.removeListener('alert-trigger', onAlertTrigger as any)` in the close cleanup
    - Compilation: `npx tsc --noEmit` exits with code 0
    - Tests: `npx vitest run` — all alert-engine tests pass (existing + new evaluate-diff tests)
    - Behavior: GET /api/alerts/config/:id returns 200 + AlertConfig for configured river, 404 + error JSON for unconfigured river
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | GET /api/alerts/config/:id — reads URL param only, returns config object |
| Client → SSE | /api/events — read-only event stream, no client input submitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | GET /api/alerts/config/:id | accept | Single-user app with no auth per project constraint. Config data contains only river IDs, type enums, and numeric thresholds — no PII or secrets. |
| T-05-02 | Tampering | PUT /api/alerts/config/:id | mitigate | Already mitigated in Phase 4 — inline validation checks type enum (level/numeric), level range (1-5 integer), customValue positive. No new PUT body endpoints in this plan. |
| T-05-03 | Information Disclosure | SSE alert events | accept | Alert events carry river IDs, numeric thresholds, and flow data — no PII. v1.1 is single-user. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan. All work uses existing dependencies. Package audit gate not required. |
</threat_model>

<verification>
1. `npx tsc --noEmit` — zero compilation errors
2. `npx vitest run --reporter=verbose` — all alert-engine tests pass (existing 17 + new evaluate-diff tests)
3. Grep `src/core/events.ts` for both `alert-trigger` and `alert-resolve` entries in ScraperEventMap
4. Grep `src/core/alert-engine.ts` for `return { triggered, resolved }` in evaluate()
5. Grep `src/index.ts` for `emit('alert-trigger'` and `emit('alert-resolve'` in data-update handler
6. Grep `server.ts` for `app.get('/api/alerts/config/:id'`, `.on('alert-trigger'`, and `.on('alert-resolve'`
</verification>

<success_criteria>
1. evaluate() returns `{ triggered, resolved }` diff on every call
2. `alert-trigger` and `alert-resolve` events emitted from data-update handler in src/index.ts
3. SSE bridge forwards both event types to all connected clients with proper cleanup on disconnect
4. GET /api/alerts/config/:id returns AlertConfig or 404
5. All 17 existing + new tests pass
6. TypeScript compilation passes with zero errors

The measurable outcome: an SSE client connected to /api/events receives `event: alert-trigger` and `event: alert-resolve` messages with valid JSON payloads. Plan 05-02 consumes these events.
</success_criteria>

<output>
`.planning/phases/05-alerts-page-ux/05-01-SUMMARY.md` when done
</output>
