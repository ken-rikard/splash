---
phase: 01-scraper-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/core/types.ts
  - src/core/adapter.ts
  - src/config.ts
  - tests/adapters/hvorerdetvann.fixtures.ts
autonomous: true
requirements:
  - SCRP-04
  - SCRP-02

must_haves:
  truths:
    - "The RiverData interface has all required fields (id, name, source, currentLevel, alertLevel, lastUpdated, status, error?)"
    - "The DatasourceAdapter interface defines sourceId + fetch() returning RiverData[]"
    - "A ScraperConfig interface exists with schedule, retry, and staleWindowMinutes fields"
    - "TypeScript compiles with strict mode and nodenext module resolution"
    - "Test fixtures contain a valid sample API response matching the API structure"
    - "package.json has type: module, dependencies p-retry and node-cron, devDeps typescript vitest tsx"
  artifacts:
    - path: package.json
      provides: "Project manifest with ESM module type and all dependencies"
    - path: tsconfig.json
      provides: "TypeScript compiler configuration (strict, nodenext)"
    - path: vitest.config.ts
      provides: "Vitest test runner configuration"
    - path: src/core/types.ts
      provides: "RiverData, ScraperStatus, and ZoneLevel type definitions"
    - path: src/core/adapter.ts
      provides: "DatasourceAdapter interface contract"
    - path: src/config.ts
      provides: "ScraperConfig interface with defaults for schedule, retry, stale window"
    - path: tests/adapters/hvorerdetvann.fixtures.ts
      provides: "Sample API response for adapter test reuse"
  key_links:
    - from: src/core/adapter.ts
      to: src/core/types.ts
      via: "imports RiverData"
      pattern: "import.*RiverData.*from.*types"
    - from: src/config.ts
      to: types.exported.fields
      via: "imports nothing directly but config values drive engine behavior"
---

<objective>
Create project foundation: scaffold, core type contracts, and test fixtures.

Purpose: Establish the TypeScript project structure, shared type definitions, adapter interface contract, configuration model, and reusable test data that all subsequent plans depend on. This is the zero-point for every other module.

Per D-01 — TypeScript on Node.js strict mode.
Per D-12 — RiverData includes id, name, source, currentLevel, alertLevel, lastUpdated, status, error? exactly as specified.
Per D-09/D-11 — DatasourceAdapter interface defines sourceId + fetch(): Promise<RiverData[]>.
Per D-04/D-05 — ScraperConfig holds schedule, retry, staleWindowMinutes config.
</objective>

<execution_context>
@/home/kenr/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/kenr/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scraper-engine/01-CONTEXT.md
@.planning/phases/01-scraper-engine/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create project scaffold: package.json, tsconfig.json, vitest.config.ts</name>
  <files>package.json, tsconfig.json, vitest.config.ts</files>

  <read_first>
    CONTEXT.md D-01 (TypeScript on Node.js)
    RESEARCH.md "Standard Stack" section listing package versions
    RESEARCH.md "ESM Import Confusion" pitfall requiring module: nodenext + .js extension
  </read_first>

  <action>
    Create the three scaffold files at project root `/home/kenr/Repos/Ken/Splash/`.

    **package.json:** type field set to "module". Dependencies: p-retry@8.0.0, node-cron@4.2.1. Dev dependencies: typescript@6.0.3, vitest@4.1.7, tsx@4.22.3. Scripts: "build" runs tsc, "test" runs vitest run --reporter=verbose, "dev" runs tsx watch src/index.ts. Do NOT include node-fetch or cheerio — they were eliminated per RESEARCH.md "Eliminated" table. Do not include any other dependencies.

    **tsconfig.json:** compilerOptions: strict=true, module=nodenext, moduleResolution=nodenext, target=es2024, outDir=dist, rootDir=src, declaration=true, sourceMap=true. include=["src"]. exclude=["node_modules", "dist", "tests"]. All imports must use .js extension per Pitfall 4.

    **vitest.config.ts:** Use `import { defineConfig } from 'vitest/config'`. Set test.glob=true, test.include=["tests/**/*.test.ts"], test.dir="./tests". Export default defineConfig(...). ESM-compatible config.

    Run `npm install` after creating package.json to install dependencies. Verify vitest resolves by running `npx vitest --version`.
  </action>

  <acceptance_criteria>
    1. `package.json` exists with "type": "module", p-retry@8.0.0, node-cron@4.2.1, typescript@6.0.3, vitest@4.1.7, tsx@4.22.3
    2. `tsconfig.json` exists with strict: true, module: "nodenext", target: "es2024"
    3. `vitest.config.ts` exists and defines test directory as "./tests"
    4. Running `npx tsc --noEmit` exits 0 (no compilation errors on empty project)
    5. Running `npx vitest --version` prints a version string (vitest is installed)
    6. package-lock.json or node_modules exists confirming dependencies installed
  </acceptance_criteria>

  <verify>
    <automated>test -f package.json && test -f tsconfig.json && test -f vitest.config.ts && test -d node_modules && npx tsc --noEmit 2>&1 | grep -v 'error' | head -5</automated>
  </verify>

  <done>Project scaffold created — tsc compiles cleanly, vitest resolves, all deps installed.</done>
</task>

<task type="auto">
  <name>Create core type definitions and DatasourceAdapter interface</name>
  <files>src/core/types.ts, src/core/adapter.ts</files>

  <read_first>
    CONTEXT.md D-12 (RiverData exact field list)
    CONTEXT.md D-09 (DatasourceAdapter interface with fetch + parse)
    CONTEXT.md D-11 (typed RiverData[] return — no raw JSON leak)
    RESEARCH.md "Pattern 1: DatasourceAdapter Interface" for exact type structures
    (created in Task 1): tsconfig.json (to confirm module resolution)
  </read_first>

  <action>
    Create `src/core/types.ts` with these exports:

    - Type alias `AlertLevel = 1 | 2 | 3 | 4 | 5` (numeric five-level scale per D-12)
    - Type alias `RiverStatus = 'ok' | 'stale' | 'error'` (per D-12)
    - Interface `RiverData` with exact fields (per D-12):
      - `id: string` — unique, e.g. "nve:1000"
      - `name: string` — human-readable river name
      - `source: string` — always "nve" for flow data
      - `stationId: string` — NVE station ID (required)
      - `currentLevel: number | null` — discharge in m³/s
      - `unit: string` — always "m³/s"
      - `alertLevel: AlertLevel` — 1-5 five-level scale position
      - `lastUpdated: Date`
      - `status: RiverStatus`
      - `error?: string` — human-readable error when status is 'error'

    - Interface `RiverEntry` (per D-13):
      - `id: string` — unique, e.g. "nve:1000"
      - `stationId: string` — NVE station ID
      - `name: string` — human-readable river name
      - `alternateNames: string[]` — names from other sources
      - `grade: string` — kayak/rafting difficulty, e.g. "III-IV"
      - `description: string` — river description
      - `guideUrl?: string` — link to guide or crucial info
      - `dangerLevels: number[]` — 5-level thresholds in m³/s
      - `enabled: boolean` — whether to monitor this river
      - `sources: string[]` — metadata sources, e.g. ["nokken", "hvorerdetvann"]

    - Interface `ScraperStatus`:
      - `sourceId: string`
      - `lastFetch: Date | null`
      - `status: 'idle' | 'fetching' | 'ok' | 'error'`
      - `error?: string`

    Create `src/core/adapter.ts` with these exports:

    - Import `RiverData` from `./types.js` (with .js extension per ESM convention)
    - Interface `DatasourceAdapter`:
      - `readonly sourceId: string`
      - `fetch(): Promise<RiverData[]>`

    Do NOT include any implementation, only the interface contract per D-09/D-11.
    Re-export `RiverData` from adapter.ts if convenient (or just import from types.js).
    
    After creating both files, run `npx tsc --noEmit` to verify compilation.
  </action>

  <acceptance_criteria>
    1. `src/core/types.ts` exports `RiverData` interface with all fields matching D-12 (id, name, source, stationId, currentLevel, unit, alertLevel, lastUpdated, status, error?)
    2. `RiverData.source` is typed as string but always "nve" at runtime
    3. `RiverData.stationId` is required (string, not optional)
    4. `src/core/types.ts` exports `RiverEntry` interface with all fields matching D-13 (id, stationId, name, alternateNames, grade, description, guideUrl?, dangerLevels, enabled, sources)
    5. `RiverData.alertLevel` is typed as `AlertLevel` union (1|2|3|4|5)
    6. `RiverData.status` is typed as `RiverStatus` union ('ok'|'stale'|'error')
    7. `src/core/adapter.ts` exports `DatasourceAdapter` interface with `sourceId` and `fetch()`
    8. `DatasourceAdapter.fetch()` return type is `Promise<RiverData[]>`
    9. `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c 'error'</automated>
  </verify>

  <done>Core types and adapter interface compile cleanly — contracts for all downstream code established.</done>
</task>

<task type="auto">
  <name>Create ScraperConfig and test fixtures for API response</name>
  <files>src/config.ts, tests/adapters/hvorerdetvann.fixtures.ts</files>

  <read_first>
    CONTEXT.md D-04 (cron configurable, default 15min)
    CONTEXT.md D-05 (retry: base 2s, cap 60s, max 3)
    CONTEXT.md D-06 (stale window: 30 min)
    RESEARCH.md Config section for exact config interface shape
    RESEARCH.md Pattern 1 (type ApiSection, ApiResponse for fixture shape)
    (created in Task 2): src/core/types.ts (RiverData interface)
    (created in Task 2): src/core/adapter.ts (DatasourceAdapter interface)
  </read_first>

  <action>
    Create `src/config.ts`:
    
    - Export interface `ScraperConfig`:
      - `schedule: string` (cron expression, default '*/15 * * * *' per D-04)
      - `scheduleTimezone: string` (default 'UTC')
      - `retry: { retries: number; minTimeout: number; maxTimeout: number; factor: number }` — defaults per D-05: retries=3, minTimeout=2000, maxTimeout=60000, factor=2
      - `staleWindowMinutes: number` (default 30 per D-06)
    
    - Export function `defaultConfig(): ScraperConfig` that reads env vars with defaults:
      - `SCRAPE_SCHEDULE` → schedule
      - `SCRAPE_SCHEDULE_TZ` → scheduleTimezone
      - `SCRAPE_RETRIES` → retry.retries (parseInt)
      - `SCRAPE_RETRY_MIN_TIMEOUT` → retry.minTimeout
      - `SCRAPE_RETRY_MAX_TIMEOUT` → retry.maxTimeout
      - `SCRAPE_STALE_WINDOW_MINUTES` → staleWindowMinutes
      - `NVE_API_KEY` → returned separately in config for the NVE adapter (if missing, NVE adapter logs warning and skips)

    - Also export `nveApiKey(): string | undefined` that reads `NVE_API_KEY` from env.
    
    Create `tests/adapters/hvorerdetvann.fixtures.ts`:
    ...
    Run `npx tsc --noEmit` to verify compilation after both files (src/config.ts and fixtures).

    Create `tests/adapters/nve.fixtures.ts`:
    - Export `const SAMPLE_OBSERVATION_RESPONSE` matching NVE HydAPI Observation response:
      - `{ data: [{ stationId: 1000, stationName: string, parameter: 1000, resolutionTime: 1440, observervations: [{ value: number, time: string }] }, ...] }`
      - At least 3 stations with realistic values (e.g., Drammenselva 22.5 m³/s, Glomma 800 m³/s, Numedalslågen 90 m³/s)
      - Each with at least one observation (newest)
    - Export `const EMPTY_OBSERVATION_RESPONSE` with empty data array
    - Export `const EXPECTED_STATION_COUNT` matching count in SAMPLE

    Run `npx tsc --noEmit` to verify compilation after all files.
  </action>

  <acceptance_criteria>
    1. `src/config.ts` exports `ScraperConfig` interface with schedule (string), scheduleTimezone (string), retry (object with retries/minTimeout/maxTimeout/factor), staleWindowMinutes (number)
    2. `src/config.ts` exports `defaultConfig()` that reads env vars and falls back to correct defaults matching D-04/D-05/D-06
    3. `tests/adapters/hvorerdetvann.fixtures.ts` exports `SAMPLE_API_RESPONSE` with ≥ 3 sections across different zone values
    4. `tests/adapters/hvorerdetvann.fixtures.ts` exports `MALFORMED_RESPONSE` object that lacks a valid sections array
    5. `tests/adapters/hvorerdetvann.fixtures.ts` exports `ZONE_TO_LEVEL_MAP` with all 5 mappings
    6. `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c 'error'</automated>
  </verify>

  <done>Config module with env-var-driven defaults and reusable API test fixtures compile cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compile-time contracts → runtime | TypeScript types compile away. The DatasourceAdapter interface is trusted at compile time but must be validated at runtime by implementations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | DatasourceAdapter interface | mitigate | Strict TypeScript (strict: true, noUncheckedIndexedAccess) enforces RiverData shape at compile time. Runtime adapters must pass type checks. |
| T-01-02 | Information Disclosure | Test fixtures in source | accept | No real credentials or secrets in fixtures — all synthetic data. Public API data only. |
| T-01-SC | Tampering | npm dependencies | mitigate | All packages verified in RESEARCH.md Package Legitimacy Audit. None have postinstall scripts. Approved disposition. |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npx vitest --version` exits 0
- [ ] All created files exist at correct paths
</verification>

<success_criteria>
- Project compiles with strict TypeScript
- Core RiverData and DatasourceAdapter contracts are defined
- Config module loads from env vars with defaults
- Test fixtures are available for downstream plan consumption
</success_criteria>

<output>
When done, create SUMMARY: `.planning/phases/01-scraper-engine/01-01-SUMMARY.md`
</output>
